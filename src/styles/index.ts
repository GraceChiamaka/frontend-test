import { DefaultTheme } from "styled-components";
import { border, borderRadius, colors, fontSize, media, spacing } from "./theme";
export * from "./ThemeRegistry";

export const theme: DefaultTheme = {
    border,
    borderRadius,
    colors,
    fontSize,
    media,
    spacing,
};
